/** AI configuration constants */

/** Max image size (bytes) to send directly to vision models. Larger files use Tesseract OCR fallback. */
export const AI_IMAGE_SIZE_LIMIT = 5 * 1024 * 1024; // 5 MB
